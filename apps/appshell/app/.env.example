# AppShell Environment Variables

# App Name — displayed in Header and navigation
VITE_APP_NAME=AppShell

# API Base URL (optional — in development, MSW intercepts all requests)
# VITE_API_BASE_URL=https://api.example.com
